import { Settings } from "lucide-react";
import { PagePlaceholder } from "../../components/PagePlaceholder";

const SettingsPage = () => {
  return <PagePlaceholder icon={Settings} title="Settings" message="Settings are being migrated to the new design. Full functionality coming soon!" />;
};

export default SettingsPage;
