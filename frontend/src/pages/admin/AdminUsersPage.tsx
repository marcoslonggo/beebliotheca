import { User } from "lucide-react";
import { PagePlaceholder } from "../../components/PagePlaceholder";

const AdminUsersPage = () => {
  return <PagePlaceholder icon={User} title="Admin Users" message="User administration is being migrated to the new design. Full functionality coming soon!" />;
};

export default AdminUsersPage;
